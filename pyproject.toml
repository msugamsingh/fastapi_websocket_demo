[project]
name = "fastapi-ws-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "sugamsingh", email = "sugam@getambee.com" }
]
requires-python = ">=3.9"
dependencies = [
    "fastapi[standard]>=0.128.8",
]

[project.scripts]
fastapi-ws-demo = "fastapi_ws_demo:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
